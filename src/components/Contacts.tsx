import { Box, Typography } from '@mui/material';

export const Contacts: React.FC = () => {
    return (
        <Box>
            <Typography
                variant='h3'
                color='text.secondary'
                textAlign='center'
            >
                Contact me
            </Typography>
        </Box>
    );
};
